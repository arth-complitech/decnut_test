<%= bootstrap_form_for(@step) do |f| %>
<% if @step.errors.any? %>
<div id="error_explanation">
  <h4><%= pluralize(@step.errors.count, "error") %> prohibited this step from being saved:</h4>
  <ul>
    <% @step.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>
<div class="row">
  <div class="col-lg-4">
    <!--
    <div class="field">
      <%#= f.label :pathway_id %><br>
      <%#= f.select :pathway_id ,options_from_collection_for_select(Pathway.all,'id','title',@step.pathway_id), :hide_label => true %>
    </div>
    -->
    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title , :hide_label => true %>
    </div>
    
    <div class="field">
      <%= f.label :body %><br>
      <%= f.text_area :body, :hide_label => true %>
    </div>
    <div class="field">
      <%= f.label :url_link %><br>
      <%= f.file_field :url_link, :hide_label => true %>
    </div>
  </div>
  <div class="col-lg-4">
    <div class="field">
      <%= f.label :parent_step %><br>
      <%= f.text_field :parent_step_id, :hide_label => true %>
    </div>
    <div class="field">
      <%= f.label :subtitle %><br>
      <%= f.text_field :subtitle, :hide_label => true %>
    </div>
    <div class="field">
      <%= f.label :memo %><br>
      <%= f.text_area :memo, :hide_label => true %>
    </div>
    
    <div class="field">
      <%= f.label :active %><br>
      <%= f.check_box :active, :hide_label => true %>
    </div>
    
  </div>
  <div class="col-lg-4">
  </div>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>