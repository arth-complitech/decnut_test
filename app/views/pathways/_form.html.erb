<%= form_for(@pathway) do |f| %>
  <% if @pathway.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pathway.errors.count, "error") %> prohibited this pathway from being saved:</h2>

      <ul>
      <% @pathway.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :active %><br>
    <%= f.check_box :active %>
  </div>
  <div class="field">
    <%= f.label :memo %><br>
    <%= f.text_area :memo %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.select :user_id ,options_from_collection_for_select(User.all,'id','first_name',@pathway.user_id) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
